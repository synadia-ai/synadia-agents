// @synadia-ai/agent-service — server-side TypeScript SDK for the NATS Agent Protocol.
//
// Pairs with @synadia-ai/agents (caller-side). Agent harness authors install both;
// this package re-exports nothing from the caller — clean, two-package boundary.
//
// Filled in by Phase 4 / 5 of the SDK split.
export {};
