# Sender-identity fixture: one nkey user whose publish permission denies
# `$SYS.>` — so `$SYS.REQ.USER.INFO` produces a permissions violation instead
# of a reply. Port-less (see nkey-noaccounts.conf). Keys: keys.json (alice).
authorization { users: [ { nkey: "UCDUW5V44EBDBIK2FL4CTNDBQFNGBEZVJHSZGQVKRHASN4AV4IWPB5NT", permissions: { publish: { deny: "$SYS.>" } } } ] }
