# Sender-identity fixture: one nkey user, no accounts (global account `$G`).
# Port-less on purpose — the test harnesses pass `-a 127.0.0.1 -p <port>`
# on the command line, which supplies the listen address (nats-server 2.12.7).
# Keys are the throwaway pairs in keys.json (alice).
authorization { users: [ { nkey: "UCDUW5V44EBDBIK2FL4CTNDBQFNGBEZVJHSZGQVKRHASN4AV4IWPB5NT" } ] }
