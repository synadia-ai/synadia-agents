"""Demo scripts ported one-for-one from the TS SDK's ``examples/`` directory.

These are user-facing examples, not part of the published distribution.
The ``_`` prefix on :mod:`examples._connect_cli` and
:mod:`examples._reference_agent` marks them as internal plumbing — the
numbered scripts are the ones users open first.
"""
