"""Demo scripts ported one-for-one from the TS SDK's ``examples/`` directory.

These are user-facing examples, not part of the published distribution.
The ``_`` prefix on :mod:`examples._connect_cli` marks it as internal
plumbing — the numbered scripts are the ones users open first. The
spec-compliant reference agent that drives these demos now ships with
the sibling :mod:`synadia_ai.agent_service` distribution at
``agent-sdk/python/examples/_reference_agent.py``.
"""
